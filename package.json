{
  "private": true,
  "scripts": {
    "deploy": "surge ./public --domain portfolio.cbayley.surge.sh",
    "start": "browser-sync start --server --startPath public --files public",
    "sass": "sass --watch public\\scss:public\\css"
  },
  "dependencies": {
    "browser-sync": "^2.23.6",
    "stylelint": "^9.1.1",
    "stylelint-config-standard": "^18.2.0",
    "surge": "^0.20.1"
  }
}
